server:
  port: 0

spring:
  application:
    name: EMPLOYEE-SERVICE
  config:
    import: 'optional:configserver:'
  cloud:
    config:
      uri: ${CONFIG_URI}

logging:
  level:
    org.springframework.security: DEBUG
